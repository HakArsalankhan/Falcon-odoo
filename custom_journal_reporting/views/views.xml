<odoo>
  <data>
    <!-- Report Action for Journal Entry -->
    <record id="action_report_journal_entry" model="ir.actions.report">
      <field name="name">Journal Entry Report</field>
      <field name="model">account.move</field>
      <field name="report_type">qweb-pdf</field>
      <field name="report_name">custom_journal_reporting.report_journal_entry</field>
      <field name="report_file">custom_journal_reporting.report_journal_entry</field>
      <field name="print_report_name">'Journal Entry - %s' % (object.name or 'Draft')</field>
      <field name="binding_model_id" ref="account.model_account_move"/>
      <field name="binding_type">report</field>
    </record>
  </data>
</odoo>
